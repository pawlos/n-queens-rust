use std::ops::BitOrAssign;

pub fn n_queens(n: usize) -> Vec<Vec<String>> {
    //results
    let results: Vec<Vec<Option<usize>>> = vec![];
    //frontier
    let frontier: Vec<Vec<Option<usize>>> = vec![];
    //populate the frontier

    //search
    //validity function

    //serializer
    vec![vec![String::from("Q")]]
}